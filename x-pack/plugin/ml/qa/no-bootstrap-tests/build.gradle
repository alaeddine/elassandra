apply plugin: 'elasticsearch.standalone-test'

dependencies {
    testCompile "com.strapdata.elasticsearch.plugin:x-pack-core:${version}"
    testCompile project(path: xpackModule('ml'), configuration: 'runtime')
}
